import { Player, Enemy } from '../types/game';

// Skill system logic

export class SkillSystem {
  processAutoSkills(player: Player, enemies: Enemy[]): void {
    // TODO: Implement auto-skill logic
  }
}
