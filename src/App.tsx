import React from 'react';
import GameBoard from './components/GameBoard';

const App: React.FC = () => {
  return (
    <div className="app">
      <GameBoard />
    </div>
  );
};

export default App;